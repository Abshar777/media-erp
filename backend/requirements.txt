# Web framework
fastapi==0.115.6
# Pinned explicitly, not left to fastapi's transitive range. This Python install
# is shared with other Delta projects; one of them pulled starlette 1.6.0 in,
# which removed `on_startup` from `Router.__init__` and made EVERY router in this
# app raise TypeError on import — the backend could not boot at all. fastapi
# 0.115.6 requires starlette>=0.40,<0.42.
starlette==0.41.3
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# Database
motor==3.6.0
pymongo==4.9.2

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
cryptography==43.0.3

# Config
pydantic-settings==2.8.1
pydantic[email]==2.11.4
python-dotenv==1.0.1

# Task queue
celery==5.4.0
redis==5.2.1
# IANA timezone database — required for Celery beat to resolve "Asia/Kolkata"
# (mediaERP runs on IST). Windows and slim containers do not ship the tzdb.
tzdata>=2024.1

# AI — Gemini (primary, cloud API) + Ollama (optional local fallback)
google-generativeai>=0.8.0
ollama>=0.6.0

# OAuth / HTTP
httpx==0.28.1
aiohttp==3.11.10

# Object storage — Cloudflare R2 (S3-compatible)
boto3==1.35.99

# Report exports (campaign Excel/PDF, group chat report PDF)
openpyxl==3.1.5
reportlab==4.5.1

# Observability (Phase 8.7 — no-op when SENTRY_DSN is empty)
sentry-sdk[fastapi]==2.19.2

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
